import React, { FC } from 'react';
import { Modal } from 'antd';

export interface Props {
    visible?: boolean;
    onClose: () => void;
}

export const EditUserRoleModal: FC = (props) => {
    return (
        <Modal>
            
        </Modal>
    )
}